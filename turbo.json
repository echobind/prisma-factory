{
  "pipeline": {
    "db:generate": {
      "outputs": ["node_modules/.prisma/client", "node_modules/.prisma-factory/factories"]
    },
    "build": {
      "dependsOn": ["db:generate", "^build"],
      "outputs": ["dist/**", ".next/**"]
    },
    "lint": {
      "outputs": []
    },
    "clean": {
      "cache": false,
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
